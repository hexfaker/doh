workdir_from_host = true
image_build_command = "earthly --build-arg IMAGE_NAME={image_name} +image"
use_local_config = true

[fake_home]
real_paths = [
  ".bash_history"
]


[hosts.dell-hexfaker]
bind_paths = []
